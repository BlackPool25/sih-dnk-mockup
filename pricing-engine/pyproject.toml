[project]
name = "pricing-engine"
version = "0.1.0"
description = "SIH DNK pricing-engine component — placeholder scaffold (pricing team owns real implementation)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<1",
    "uvicorn>=0.30,<1",
]
