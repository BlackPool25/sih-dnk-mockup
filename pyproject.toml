[project]
name = "sih-dnk-mockup"
version = "0.1.0"
description = "SIH DNK export-enablement mockup — scaffold (models/schemas/services seeded in later todos)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy>=2.0.30,<2.1",
    "psycopg[binary]>=3.1,<4",
    "alembic>=1.13,<2",
    "pydantic>=2.7,<3",
    "python-dotenv>=1.0",
    "pyyaml>=6",
    "jinja2>=3.1,<4",
    "weasyprint>=62,<64",
    "pycountry>=24.6",
    "pytest>=8",
    "pytest-cov>=5",
]
