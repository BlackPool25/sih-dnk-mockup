import ShipmentTimeline from "./ShipmentTimeline";

// Deprecated: use ShipmentTimeline directly. Kept for backward compat.
// This re-export ensures old imports still work while polling is unified.
export default ShipmentTimeline;
export { ShipmentTimeline };
